# ============================================
# CLEANED REQUIREMENTS - PaddleOCR 2.7.x (noavx-capable)
# PaddlePaddle 3.x has no official noavx Windows wheel; 2.6.x is the
# last line with noavx support. See installer/setup_neurox.bat for
# AVX detection + conditional paddlepaddle wheel selection.
# ============================================

# Core Web Framework
fastapi>=0.115,<1
uvicorn>=0.34,<1
python-multipart>=0.0.18
python-dotenv>=1.1,<2
pydantic>=2.10,<3

# CORS & HTTP
httpx>=0.28,<1
requests>=2.32,<3

# Database (PostgreSQL)
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
asyncpg>=0.30,<1
alembic>=1.14,<2

# OCR - PaddleOCR 2.7.x (supports CPUs without AVX via paddlepaddle-noavx)
paddleocr==2.7.3
paddlepaddle==2.6.2

# Image Processing
# Pillow pinned <11 for PaddleOCR 2.7 compatibility
Pillow>=10.3,<11
opencv-python>=4.9,<5
# NumPy pinned <2 for PaddlePaddle 2.6 compatibility
numpy>=1.26,<2
# Protobuf pinned <4 for PaddlePaddle 2.6 compatibility
protobuf>=3.20,<4

# PDF Processing
PyMuPDF>=1.24,<2
pdf2image>=1.17,<2

# Template filling
python-docx>=1.1,<2

# OpenAI API (for extraction fallback)
openai>=1.50,<2

# Authentication
passlib[bcrypt]>=1.7,<2
bcrypt>=4.0,<6

# Utilities
loguru>=0.7,<1
python-dateutil>=2.9,<3
pydantic-settings>=2.7,<3
py-cpuinfo>=9,<10

# PaddleOCR 2.7 runtime deps
shapely>=2,<3
pyclipper>=1.3,<2
imgaug>=0.4,<1
lmdb>=1.4,<2
rapidfuzz>=3,<4

# Development
pytest>=8,<9
pytest-asyncio>=0.25,<1
